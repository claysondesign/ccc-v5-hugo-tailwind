{
	"name": "centcolschallenge",
	"version": "5.0",
	"description": "Website for CCC",
	"main": "index.js",
	"repository": "https://bitbucket.org/claypole/ccc-v5-hugo-tailwind/",
	"author": "Simon CLayson <sc@clayson.design>",
	"license": "MIT",
	"scripts": {
		"dev": "hugo server --disableFastRender",
		"dev:example": "hugo server --verbose -s centcols --themesDir=../.. --disableFastRender",
		"build": "NODE_ENV=production hugo --verbose --gc --minify",
		"build:example": "NODE_ENV=production hugo --verbose --gc --minify -s centcols --themesDir=../..",
		"start": "hugo --gc"
	},
	"devDependencies": {
		"autoprefixer": "^10.1.0",
		"postcss": "^8.2.2",
		"postcss-cli": "^8.3.1",
		"postcss-import": "^13.0.0",
		"tailwindcss": "^2.0.2"
	},
	"browserslist": [
		"last 1 version",
		"> 1%",
		"maintained node versions",
		"not dead"
	],
	"dependencies": {
		"macy": "^2.5.1"
	}
}
